---
description: 当新增功能、修改 API 接口、变更数据结构时，自动提醒同步 PRD 文档
globs:
  - "backend/app/routers/**/*.py"
  - "backend/app/models/**/*.py"
  - "mobile/app/**/*.tsx"
  - "mobile/services/**/*.ts"
  - "mobile/stores/**/*.ts"
alwaysApply: false
---

# PRD 同步提醒规则

每当对以下文件做了实质性变更（新增功能、修改接口、变更数据结构），**必须同步更新 PRD**。

## 需要同步的场景

| 代码变更 | 需要更新 PRD 的哪里 |
|----------|---------------------|
| 新增 API 路由（routers/） | §5 核心功能 + §14 开发优先级 |
| 新增/修改数据库模型（models/） | §8 数据结构 |
| 新增移动端页面（mobile/app/） | §7 页面结构 + §14 开发优先级 |
| 新增 Service / Store | §5 核心功能 描述 |
| 功能上线（P0/P1 完成） | §14 将 `- [ ]` 改为 `- [x] ✅` |

## 同步步骤

1. 打开 `PRD-不落灰-V0.1.md`
2. 根据上表找到对应章节，更新内容
3. 在顶部"变更记录"表格新增一行，填写版本号（V0.x.y）、日期、变更说明
4. 提交时在 commit body 中注明"文档：同步 PRD §xx"

## 版本号规则

- **功能上线**（P0/P1 完成）→ 小版本 +1，如 V0.2 → V0.3
- **文档修正 / 状态同步** → patch +1，如 V0.2.1 → V0.2.2
- **新增重大模块**（扩展模块）→ 中版本 +1，如 V0.x → V1.0

## 当前文档位置

`PRD-不落灰-V0.1.md` — 根目录，当前版本 V0.2.1

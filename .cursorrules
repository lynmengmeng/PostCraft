## Git Commit 信息规范

生成 git commit 信息时，严格遵守以下规则：

- 格式遵循 Conventional Commits 1.0.0 规范
- `type` 和 `scope` 保持英文（如 feat、fix、chore、refactor、docs、style、test、perf、ci）
- 冒号后的描述（subject）**必须使用简体中文**，禁止使用英文
- 正文（body）和脚注（footer）同样使用简体中文
- issue 编号、URL、文件路径、技术标识符保留原样，不翻译
- 标题不超过 72 字符，使用祈使句，不加句号
- 破坏性变更在 type 后加 `!`，并在脚注添加 `BREAKING CHANGE: <中文说明>`

示例：
```
feat(auth): 新增手机号一键登录功能
fix(upload): 修复大文件上传时进度条卡死问题
chore: 升级依赖至最新稳定版本
```
